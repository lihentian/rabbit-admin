/** 包铁/包团角标尺寸（px）— 卡片 */
export const COVER_BADGE_SIZE_CARD = 16;

/** 包铁/包团角标尺寸（px）— 菜单 */
export const COVER_BADGE_SIZE_MENU = 20;

/** 成员卡片内容区高度（px） */
export const MEMBER_CARD_HEIGHT = 56;

/** 显示账号备注时的卡片高度（px） */
export const MEMBER_CARD_HEIGHT_WITH_REMARK = 70;

/** 队伍界面小队列最大宽度（px） */
export const SQUAD_COLUMN_MAX_WIDTH = 168;

/** 角色池卡片最小列宽（px） */
export const POOL_CARD_MIN_WIDTH = 168;

/** 角色池面板最小宽度（px） */
export const POOL_PANEL_MIN_WIDTH = 200;
